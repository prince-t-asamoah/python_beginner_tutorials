language: python
python:
  - "3.13"
  - "3.7"
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover

# Read flake8 config file
matrix:
  include:
    - python: "3.13"
      script: "flake8"